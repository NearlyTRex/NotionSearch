fastapi==0.141.1
# Pinned explicitly although FastAPI pulls it in: FastAPI only requires
# starlette>=0.46.0, and older versions carry CVE-2026-48818 (SSRF and NTLM
# credential theft via UNC paths in StaticFiles, which this app uses to serve
# web/) plus two DoS issues. Pinning keeps that decision visible and auditable.
starlette==1.6.0
uvicorn[standard]==0.52.4
httpx==0.28.1
meilisearch==0.33.0
itsdangerous==2.2.0
